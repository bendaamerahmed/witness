# The mount that ships this repo to Windows reports every file as executable and
# would otherwise translate line endings. Normalize both so a fresh clone on any
# platform has a clean working tree.
* text=auto eol=lf

*.png binary
*.zip binary
*.sh  text eol=lf
*.ps1 text eol=crlf
